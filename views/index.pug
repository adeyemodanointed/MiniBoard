extends layout

block content
  h1= title
  h3 Welcome to #{title}
  - for (let i = 0; i < messages.length; i++)
    p= 'Message: ' + (messages[i].text)
    p= 'Sender: ' + (messages[i].user)
    p= 'Date: ' + (messages[i].added)
